export function ptToHalfPt(pt: number): number {
  return pt * 2
}

export function ptToTwip(pt: number): number {
  return pt * 20
}

export function lineHeightTimesToNumber(lineHeight: number): number {
  return 240 * lineHeight
}
